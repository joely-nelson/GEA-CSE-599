This is also binary classification.

But here: we are not only extracting one subsequence from each plasmid, but multiple subsequences to get about 30,000 training examples. Make sure they are still balanced.


